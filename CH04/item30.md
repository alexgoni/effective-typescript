## 아이템 30: 문서에 타입 정보를 쓰지 않기

함수의 입력과 출력의 타입을 코드로 표현하는 것이 주석보다 더 나은 방법이다.

최악의 경우 타입 정보에 모순이 발생한다.

주석을 써야한다면 설명 정도만 작성하는 것이 최선이다.

```ts
/** 특정 페이지의 전경색을 가져옵니다. */
function getForegroundColor(page?: string): Color {
  // ...
}
```
